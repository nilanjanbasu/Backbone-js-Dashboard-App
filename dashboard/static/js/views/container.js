var app = app || {};

$(function(){
	'use strict';
	
	app.finalView = Backbone.View.extend({
		
		el:'body',
		
		initialize: function(){
			
//			var path = window.location.pathname.slice(1);
//			if(path[path.length-1] === '/' )
//				path=path.slice(0,path.length-1);
			
			
			
			
			this.model.
		}
	});
});